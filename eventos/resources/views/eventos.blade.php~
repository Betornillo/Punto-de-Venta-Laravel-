<html>
  <head>
    <meta charset="utf-8">
    <title>Colores</title>
    <style media="screen">
      table{
        width: 80%;
        margin-left: 10%;
        text-align: center;
      }
      td, th{
        padding: 8px 0;
      }
    </style>
  </head>
  <body>
    <table>
      <thead>
        <th>Id</th>
        <th>Nombre</th>
        <th>Fecha</th>
        <th>Costo</th>
        <th>Asiento</th>
      </thead>
      @foreach($eventos as $evento)
          <td>{{ $evento->id }}</td>
          <td>{{ $evento->nombree }}</td>
          <td>{{ $evento->fechae }}</td>
          <td>{{ $evento->costoe }}</td>
          <td>{{ $evento->tipoe }}</td>
        </tr>
      @endforeach
    </table>
  </body>
</html>
